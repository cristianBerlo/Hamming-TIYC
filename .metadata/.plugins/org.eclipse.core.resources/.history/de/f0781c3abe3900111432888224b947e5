package hamming;

import java.math.BigInteger;
import java.util.BitSet;

public class Hamming {
	private int control; // cantidad de bits de control
	private int lenght; //largo del mensaje resultante (informacion + controles) 	
public byte[] hamming(byte[] cod) {
	int bits = 32 - Integer.numberOfLeadingZeros(lenght); //cantidad el numero de bits del fragmento
	// cantidad de controles = 2^c >= (c+i)+1 
	// cada control c ve desde la posicion 2^c en adelante
	BitSet hamming = new BitSet(tamaño);
	byte[] hamming = new byte[lenght/8]; // arreglo de bits donde va a estar el mensaje hamminizado
	 byte[] mask = new byte[(bits+7)/8]; //genero un arreglo con la cantidad total de bytes del fragmento
	for(int i=0;i<control;i++) { //itero por cada control
     java.util.Arrays.fill(mask, (byte)0);
     int byteI = i/8; //posicion en el arreglo
     int bitI =  i%8; // posicion en el bit
     int des = (int) Math.pow(2, i); // desplazamiento del bit de control
     mask[byteI] = (byte)(1 << bitI); 
	 
	  
	}
	return null;
}
}
