package hamming;

import java.math.BigInteger;

public class Hamming {
	private int control;
public String hamming(byte cod) {
	for(int i=0;i<control;i++) {
		String mask = BigInteger.ONE.shiftLeft(i).toString(2); //genero mascara
		mascara = (mascara << 1) & mascara;

	}
	return null;
}
}
