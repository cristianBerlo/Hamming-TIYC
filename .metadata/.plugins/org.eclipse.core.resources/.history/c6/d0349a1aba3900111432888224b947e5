package hamming;

import java.math.BigInteger;

public class Hamming {
	private int control;
	private int lenght;	
public String hamming(byte cod) {
	int bits = 32 - Integer.numberOfLeadingZeros(lenght);	
	// tengo 2
	// cantidad de controles = 2^c >= (c+i)+1
	for(int i=0;i<control;i++) {
	 byte[] mask = new byte[(bits+7)/8]; //genero mascara
	
	}
	return null;
}
}
