package hamming;

import java.math.BigInteger;

public class Hamming {
	private int control;
	private int lenght;	
public String hamming(byte cod) {
	int bits = 32 - Integer.numberOfLeadingZeros(lenght); //cantidad el numero de bits del fragmento
	// tengo 2
	// cantidad de controles = 2^c >= (c+i)+1 
	// cada control c ve desde la posicion 2^c en adelante
	byte[] hamming = new byte[lenght/8];
	for(int i=0;i<control;i++) {
	 byte[] mask = new byte[(bits+7)/8]; //genero un arreglo con la cantidad total de bytes del fragmento
	
	}
	return null;
}
}
